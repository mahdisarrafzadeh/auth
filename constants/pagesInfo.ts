export const pagesInfo = Object.freeze({
  authPage: {
    href: (_?: unknown) => `/auth/${_}`,
    path: "/auth",
  },
  dashboardPage: {
    href: (_?: unknown) => `/dashboard/${_}`,
    path: "/dashboard",
  },
});
